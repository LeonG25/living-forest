# Tree background — luminous branch series (Nano Banana 2)
Drop the 4 images here with EXACTLY these names (order = crossfade sequence):
  frame-1.png
  frame-2.png
  frame-3.png
  frame-4.png
The Tree page crossfades through them in a slow loop (living, colour-shifting base),
with drifting particles + gentle sway layered on top in code.
Recommended: keep each under ~2 MB (they're 1408px wide, fine). PNG or JPG both ok
(if JPG, name them frame-1.jpg etc. and tell Claude so the code matches).
